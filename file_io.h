// FILE INPUT AND OUTPUT =======================================================
#include <iostream>
#include <string>
#include <map>

void inputData()
{
    int currentYear
    map<player> playerMap;

    fin.open("data.txt");
    fin << currentYear;
    fin.get();  // remove endline character
    if
}
